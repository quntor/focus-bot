# Фокус — напарник по рабочим сессиям

Telegram-бот, который сидит рядом, пока ты работаешь. Спрашивает, над чем сейчас
работаешь, проверяется в середине сессии и разбирает отчёт в конце. Помнит, что
было вчера.

Начать труднее, чем сделать: человек знает, что нужно делать, и не садится, потому
что никто не ждёт результата. Таймер и трекер фиксируют провал, но не спрашивают,
над чем ты работаешь, и не помогают вернуться после срыва. Живой напарник это
решает, но требует расписания, языка, подписки и камеры.

**Статус: начало разработки, сентябрь 2026.** Публично ничего ещё не работает,
план по неделям — в [docs/roadmap.md](docs/roadmap.md).

## Цикл сессии

1. `/focus` — «над чем сейчас работаешь?». Человек формулирует задачу словами:
   проговорённое кому-то намерение держится сильнее.
2. Идёт таймер. В середине — короткий пинг «на месте?». Ответ возвращает внимание.
3. Финиш — «что сделал?». Отчёт одной строкой.
4. Вечером — сводка дня с подтверждением: сколько сессий, где сорвался, какая серия.

Между сессиями остаётся память по задачам: бот видит, что одна и та же задача не
двигается третью сессию подряд, и знает, на каком типе работы человек стабильно
срывается.

## Что делает модель, а что намеренно нет

Модель: принимает намерение свободным текстом и связывает его с существующими
задачами, разбирает отчёт, находит застревание, собирает вечернюю сводку, ведёт
тон разговора.

Без модели: таймер, расписание пингов, текст самого пинга, начисление очков,
серия, учёт событий и метрики. Причины две. Экономика — у вовлечённого
пользователя больше тридцати касаний в день, и каждое лишнее обращение к модели
умножается на размер аудитории. Проверяемость — метрики и начисления должны быть
воспроизводимыми, а не результатом решения модели.

## Стек

TypeScript, Node 24, Telegram Bot API через вебхук. PostgreSQL, Prisma. Отложенные
пинги и сводки — очередь в таблице с ключом идемпотентности, а не таймеры в
процессе: перезапуск приложения не должен терять сообщения. Аналитика своя, в той
же базе. Тесты — vitest. Контейнер и CI на GitHub Actions.

## Локальный запуск

Нужны Node 24+ и PostgreSQL (в docker-compose он уже есть).

```bash
npm install
cp .env.example .env   # заполнить токен бота и строку подключения
docker compose up -d db
npx prisma migrate dev
npm run dev
```

Вебхук слушает `POST /tg/<TELEGRAM_WEBHOOK_SECRET>`; проверка живости — `GET /healthz`.
Для локальной разработки адрес пробрасывается любым туннелем и регистрируется
через `setWebhook` с тем же секретом.

## Структура

```
src/tg          вебхук, разбор апдейтов, вызовы Telegram API
src/analytics   журнал событий: типы, запись, что считается обращением
src/lib         конфигурация, доступ к базе, границы суток
prisma          модель данных
docs            схема событий и метрики, план по неделям
```

## Документация

- [docs/metrics.md](docs/metrics.md) — схема событий, как считаются DAU и обращения,
  гигиена начислений;
- [docs/roadmap.md](docs/roadmap.md) — план по неделям до 15 октября.

## Права

Все права защищены. Код открыт для чтения, разрешения на использование,
копирование или распространение не даётся.
